spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/contract_lifecycle?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai}
    username: ${DB_USERNAME:contract_app}
    password: ${DB_PASSWORD:development-db-password}

app:
  jwt:
    secret: ${APP_JWT_SECRET:change-me-jwt-secret-for-local-only}
